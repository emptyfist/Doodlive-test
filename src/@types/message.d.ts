import {
  serverTimestamp
} from 'firebase/firestore'

export type MessageData = {
  userName: string,
  // userId: uid,
  // userPhotoURL: string,
  // text: string,
  // createdAt: Firebase.ServerValue.TIMESTAMP
}